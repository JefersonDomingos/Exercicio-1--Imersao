PROJETO MINI LOJA COM JavaScript
1-Construção dos produtos
const items = [     //Um array de objetos
    {
        id:0,       
        nome:'Manto Dois',       
        img:'IMG/camisaFla-branca.png',
        quantidade: 0,
    },

    {
        id:1,
        nome:'Manto Um',
        img:'IMG/camisaFla-vermelha.png',
        quantidade: 0,
    },

    {
        id:2,
        nome:'Short Treino preto',
        img:'IMG/shortFla-preto.png',
        quantidade: 0,
    },

    {
        id:3,
        nome:'Copo Térmico',
        img:'IMG/copoFla-azul.png',
        quantidade: 0,
    },

    {
        id:4,
        nome:'Chinelo Infantil',
        img:'IMG/chineloFla.png',
        quantidade: 0,
    },
    {
        id:5,
        nome:'Chaveiro da logo',
        img:'IMG/chaveiroFla.jpg',
        quantidade: 0,
    },
]  //lista de objetos e suas propriedades concluida.


inicializarLoja = () => {   //arrow function o mesmo que função
   
// variável com o caminho da <div id=”produtos”> do index.HTML
 
 var parteProdutos = document.getElementById('produtos');
    
// vamos pegar cada objeto dentro do array items
for(let i = 0; i < items.length; i++){
        console.log(items[i].nome); //imprimir o “nome” de cada um.
        
/*vamos inserir com innerHTML uma <div> dentro da <div id=”produtos”> usando o template literals ``,
Contendo : uma imagem referente a cada objeto da nossa lista,
           Um paragrafo contendo o nome que está no objeto,
 	     Um link escrito adicionar ao carrinho e com um atributo ‘key’
	     que recebe o valor do ‘id’ de cada objeto...
Então vai aparecer na tela ‘todos’ os objetos com suas imagens, nomes, links
Por que em produtos estou “concatenando” += caso contrário só mostrava UM dos objetos...*/
        
        parteProdutos.innerHTML += /*concatenar importante*/ ` 
        <div class="produto-apenas">
            <img src ="`+items[i].img+`"></img>
            <p>`+items[i].nome+`</p>
            <a class="adicionar-carrinho" key="`+items[i].id+`" href="#">Adicionar ao carrinho</a>
        </div>
        `;
    } //fechando o for(){}
  
}
inicializarLoja() //chamando a função vai colocar todos os objetos na tela


atualizarCarrinho = ()=>{
    console.log(items) 
}

var addCarrinho = document.getElementsByClassName('adicionar-carrinho'); //todos os links de add ao carrinho

for(let i = 0; i < addCarrinho.length; i++){
    addCarrinho[i].addEventListener('click',function(){
        let key = this.getAttribute('key'); //corresponde a cada id que passar em key de <a> do loop em items.
        items[key].quantidade++;
        atualizarCarrinho()
        return false; //nesse caso só para não atualizar a pagina

    })
}

atualizarCarrinho = ()=>{  //Quando essa função for chamada vai imprimir no console o array items que contém os objetos
    console.log(items) 
}
var addCarrinho = document.getElementsByClassName('adicionar-carrinho'); //todos os links de add ao carrinho


